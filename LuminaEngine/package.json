{
  "name": "luminaengine",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder",
    "build:wasm": "wasm-pack build crates/lumina-meshing --target web --out-dir ../../engine/wasm/lumina-meshing --release && wasm-pack build crates/lumina-worldgen --target web --out-dir ../../engine/wasm/lumina-worldgen --release && wasm-pack build crates/lumina-save --target web --out-dir ../../engine/wasm/lumina-save --release",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.sl1dee36.luminaengine",
    "productName": "LuminaEngine dev",
    "files": [
      "**/*",
      "!node_modules/**/*"
    ],
    "directories": {
      "output": "release/"
    }
  }
}
